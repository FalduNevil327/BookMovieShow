@{
    ViewData["Title"] = "State List";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}


@using System.Data;
@model DataTable;
@using BookMovieShow.Areas.Admin.Model
@{
    LOC_StateFilterModel FilterModel = new LOC_StateFilterModel();

}

<main id="main" class="main">

    @if (TempData["Msg"] != null)
    {
        <div id="Feature-alert" class="alert alert-success color-success">
            <i class="bi bi-check-circle"></i> @TempData["Msg"]
        </div>

        <script>
            setTimeout(function () {
                $('#Feature-alert').fadeOut('fast');
            }, 2000); // 2 seconds
        </script>
    }

    <!--Heading-->
    <div class="pagetitle">
        <h1>State List</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a asp-area="Admin" asp-controller="Admin" asp-action="Admin_DashBoard">Home</a></li>
                <li class="breadcrumb-item active">StateList</li>
            </ol>
        </nav>
    </div>
    <!--End Heading-->
    <!--Filter-->
    <section class="section">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Filter</h5>

                        <!-- No Labels Form -->
                        <form class="row g-3" method="post" asp-area="Admin" asp-controller="LOC_State" asp-action="LOC_StateFilter">

                            <div class="col-md-4">
                                <input type="text" class="form-control" placeholder="StateName" asp-for="@FilterModel.StateName">
                            </div>
                            <div class="col-md-4">
                                <input type="text" class="form-control" placeholder="StateCode" asp-for="@FilterModel.StateCode">
                            </div>

                            <div class="col-md-4">
                                <button type="submit" class="btn btn-primary">Search</button>
                                <button type="reset" class="btn btn-secondary">Reset</button>
                            </div>
                        </form><!-- End No Labels Form -->

                    </div>

                </div>
            </div>
        </div>
    </section>
    <!--End Filter-->
    <!--Start ListPage-->
    <section class="section">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <!--External button-->
                    <div class="d-flex justify-content-between card-header">
                        <div class="col-md-4" style="margin-inline-start: auto;">
                            <a type="button" class="btn btn-outline-dark" asp-area="Admin" asp-controller="LOC_State" asp-action="LOC_StateAdd">
                                Add
                            </a>

                            <a type="button" class="btn btn-outline-primary" asp-area="Admin" asp-controller="LOC_State" asp-action="State_ExportSToExcel">
                                Export
                            </a>

                            <a type="button" class="btn btn-outline-danger" id="deleteSelectedBtn" asp-area="Admin" asp-controller="LOC_State" asp-action="LOC_StateMultipleDelete">Multiple Delete</a>
                        </div>

                    </div>
                    <!--End External button-->

                    <div class="card-body">

                        <!-- Table with hoverable rows -->
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    @*<th>Check</th>*@
                                    <th><input type="checkbox" class="selectall" name="sample"></th>
                                    <th scope="col">State Name</th>
                                    <th scope="col">State Code</th>
                                    <th scope="col">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (DataRow dr in Model.Rows)
                                {
                                    <tr>
                                        <td><input type="checkbox" class="chkDelete" name="chkDeleteName" value="@dr["StateID"]"></td>
                                        <td>@dr["StateName"]</td>
                                        <td>@dr["StateCode"]</td>
                                        <td>
                                            <div class="d-flex" style="justify-content: center;">

                                                <div class="me-2">
                                                    <form method="post" asp-area="Admin" asp-controller="LOC_State" asp-action="LOC_StateDelete">
                                                        <input type="hidden" value="@Convert.ToInt32(dr["StateID"])" name="CityID" />

                                                        <button type="submit" class="btn btn-danger" value="Delete" onclick="return fun1();"><i class="bi bi-bucket"></i></button>
                                                    </form>
                                                </div>
                                                <div class="ms-2">
                                                    <form asp-area="Admin" asp-controller="LOC_State" asp-action="LOC_StateAdd">
                                                        <input type="hidden" value="@Convert.ToInt32(dr["StateID"])" name="StateID" />
                                                        <button type="submit" class="btn btn-success"><i class="bi bi-pen"></i></button>
                                                    </form>
                                                </div>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <!-- End Table with hoverable rows -->

                    </div>
                </div>
            </div>
        </div>
    </section>
    <!--End ListPage-->
</main>

@section Scripts
{
    <!--alert-->
    <script>
        function fun1() {
            if (confirm("Are you sure you want to delete?")) {
                return true;
            }
            else {
                return false;
            }
        }
        window.onload = function () {
            setTimeout(function () {
                var alert = document.getElementById('tempAlert');
                if (alert) {
                    var bsAlert = new bootstrap.Alert(alert);
                    bsAlert.close();
                }
            }, 5000);  // 5000 milliseconds = 5 seconds
        };
    </script>
    <!--End Alert-->

    <!--Multiple Delete-->
    <script>
        $(document).ready(function () {
            console.log("Hello");
            $("#deleteSelectedBtn").on("click", function (e) {
                var confirmation = confirm("Are you sure?")
                if (confirmation) {

                    e.preventDefault();
                    var data = [];
                    $("input[name='chkDeleteName']:checked").each(function () {
                        data.push($(this).val());
                    });

                    $.ajax({
                        type: "POST",
                        url: "@Url.Action("LOC_StateMultipleDelete")",
                        data: { id: data },
                        traditional: true,
                        success: function (result) {
                            //alert("done");
                            alert("Delete Success")
                            location.reload();
                        }
                    })
                }
                else
                    alert("Delete Canceled")
            })
        })

    </script>
    <!--End Multiple Delete-->

    <!--SelectAll-->
    <script>
        $('.selectall').click(function () {
            if ($(this).is(':checked')) {
                $('td input').attr('checked', true);
            } else {
                $('td input').attr('checked', false);
            }
        });
    </script>
    <!--End SelectAll-->
}